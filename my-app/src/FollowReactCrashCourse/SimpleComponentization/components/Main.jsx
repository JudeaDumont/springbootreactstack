


// figma tutorial

// figma design file

//go to figma.com and create a new account

//create a components folder, will have two files, main and navbar
//appc omponent outside components folder

// import app component then use react dom render to render it

// google font
// then put the links to the fonts above the style.css import in index.html. need to use the <link> element for fonts.

// not dealing with sting right now. just a navbar and main section on a white webpage.

import React from "react"
export default function Main(){
    return <h1>Main Component</h1>
}