
//index.js
import React from "react"
import ReactDOM from "react-dom"
import App from "./App"

ReactDOM.render(<App/>, document.getElementById("root"))

//we want to link the font links above the syle.css so we can reference the fonts in the style.css file.

//figma fiel where you have access to tdesign elements, spacing, etc.

// figma is a shared file that you can localize by saving to your drafts.

// advice: ref figma file, fonts, spacing, etc

//make the navbar look like the design displayed.